--- harfbuzz-1.0.6/configure	2015-10-16 00:24:55.274902643 +0000
+++ harfbuzz-1.0.6/configure	2018-08-24 12:13:37.215913487 +0000
@@ -18358,12 +18358,12 @@
         pkg_cv_ICU_CFLAGS="$ICU_CFLAGS"
     else
         if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"icu-uc\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "icu-uc") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"icu-uc_gcc\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "icu-uc_gcc") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_ICU_CFLAGS=`$PKG_CONFIG --cflags "icu-uc" 2>/dev/null`
+  pkg_cv_ICU_CFLAGS=`$PKG_CONFIG --cflags "icu-uc_gcc" 2>/dev/null`
 else
   pkg_failed=yes
 fi
@@ -18376,12 +18376,12 @@
         pkg_cv_ICU_LIBS="$ICU_LIBS"
     else
         if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"icu-uc\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "icu-uc") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"icu-uc_gcc\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "icu-uc_gcc") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_ICU_LIBS=`$PKG_CONFIG --libs "icu-uc" 2>/dev/null`
+  pkg_cv_ICU_LIBS=`$PKG_CONFIG --libs "icu-uc_gcc" 2>/dev/null`
 else
   pkg_failed=yes
 fi
@@ -18400,9 +18400,9 @@
         _pkg_short_errors_supported=no
 fi
         if test $_pkg_short_errors_supported = yes; then
-	        ICU_PKG_ERRORS=`$PKG_CONFIG --short-errors --errors-to-stdout --print-errors "icu-uc"`
+	        ICU_PKG_ERRORS=`$PKG_CONFIG --short-errors --errors-to-stdout --print-errors "icu-uc_gcc"`
         else
-	        ICU_PKG_ERRORS=`$PKG_CONFIG --errors-to-stdout --print-errors "icu-uc"`
+	        ICU_PKG_ERRORS=`$PKG_CONFIG --errors-to-stdout --print-errors "icu-uc_gcc"`
         fi
 	# Put the nasty error message in config.log where it belongs
 	echo "$ICU_PKG_ERRORS" >&5
@@ -18533,10 +18533,10 @@
 	fi
 fi
 if test "x$with_icu" = "xyes" -a "x$have_icu" != "xtrue"; then
-	as_fn_error $? "icu support requested but icu-uc not found" "$LINENO" 5
+	as_fn_error $? "icu support requested but icu-uc_gcc not found" "$LINENO" 5
 fi
 if $have_icu; then
-	CXXFLAGS="$CXXFLAGS `$PKG_CONFIG --variable=CXXFLAGS icu-uc`"
+	CXXFLAGS="$CXXFLAGS `$PKG_CONFIG --variable=CXXFLAGS icu-uc_gcc`"
 
 $as_echo "#define HAVE_ICU 1" >>confdefs.h
 
