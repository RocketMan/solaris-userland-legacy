Retain the last used sidebar view

See https://bugzilla.mozilla.org/show_bug.cgi?id=1922777

--- firefox-140.0/browser/components/sidebar/browser-sidebar.js~	2025-06-17 17:15:11.000000000 +0000
+++ firefox-140.0/browser/components/sidebar/browser-sidebar.js	2025-06-28 16:05:25.316587609 +0000
@@ -532,12 +532,7 @@
     if (this.inSingleTabWindow) {
       return null;
     }
-    let snapshot = this._state.getProperties();
-    // we don't persist the sidebar command when the panel is closed
-    if (!this._state.panelOpen) {
-      delete snapshot.command;
-    }
-    return snapshot;
+    return this._state.getProperties();
   },
 
   /**
