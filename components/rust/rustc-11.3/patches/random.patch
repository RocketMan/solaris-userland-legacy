Solaris 11.3 does not have arc4random(3), so we use getentropy(2)

--- rustc-1.83.0-src/library/std/src/sys/random/mod.rs-	2025-01-08 16:32:41.503755843 +0000
+++ rustc-1.83.0-src/library/std/src/sys/random/mod.rs	2025-01-08 16:34:24.352014685 +0000
@@ -17,12 +17,14 @@
         target_os = "illumos",
         target_os = "netbsd",
         target_os = "openbsd",
-        target_os = "solaris",
         target_os = "vita",
     ))] {
         mod arc4random;
         pub use arc4random::fill_bytes;
-    } else if #[cfg(target_os = "emscripten")] {
+    } else if #[cfg(any(
+        target_os = "emscripten",
+        target_os = "solaris",
+    ))] {
         mod getentropy;
         pub use getentropy::fill_bytes;
     } else if #[cfg(target_os = "espidf")] {
