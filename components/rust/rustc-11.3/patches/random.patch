Solaris 11.3 does not have arc4random(3), so we use getentropy(2)

--- rustc-1.91.0-src/library/std/src/sys/random/mod.rs.~1~	2025-10-31 19:06:07.051571499 +0000
+++ rustc-1.91.0-src/library/std/src/sys/random/mod.rs	2025-10-31 19:08:16.183901257 +0000
@@ -21,14 +21,16 @@
         target_os = "netbsd",
         target_os = "openbsd",
         target_os = "rtems",
-        target_os = "solaris",
         target_os = "vita",
         target_os = "nuttx",
     ) => {
         mod arc4random;
         pub use arc4random::fill_bytes;
     }
-    target_os = "emscripten" => {
+    any(
+        target_os = "emscripten",
+        target_os = "solaris",
+    ) => {
         mod getentropy;
         pub use getentropy::fill_bytes;
     }
