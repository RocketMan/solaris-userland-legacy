--- rustc-1.86.0-src/library/std/src/sys/pal/unix/mod.rs.~1~	2025-04-03 11:06:13.094850783 +0000
+++ rustc-1.86.0-src/library/std/src/sys/pal/unix/mod.rs	2025-04-03 11:07:36.928278011 +0000
@@ -390,6 +390,7 @@
         #[link(name = "posix4")]
         #[link(name = "pthread")]
         #[link(name = "resolv")]
+        #[link(name = "nsl")]
         unsafe extern "C" {}
     } else if #[cfg(target_os = "illumos")] {
         #[link(name = "socket")]
--- rustc-1.93.0-src/compiler/rustc_target/src/spec/base/solaris.rs.~1~	2026-01-19 16:34:28.000000000 +0000
+++ rustc-1.93.0-src/compiler/rustc_target/src/spec/base/solaris.rs	2026-01-23 16:04:48.035736426 +0000
@@ -1,6 +1,38 @@
-use crate::spec::{Cc, LinkerFlavor, Os, TargetOptions, cvs};
+use crate::spec::{LinkArgs, FramePointer, Cc, LinkerFlavor, Os, TargetOptions, cvs};
 
 pub(crate) fn opts() -> TargetOptions {
+    let mut late_link_args = LinkArgs::new();
+    late_link_args.insert(
+        LinkerFlavor::Unix(Cc::Yes),
+        vec![
+            // Rust crates (via libc) may expect various XPG entry points;
+            // on Solaris 11.3, we redirect via shims.
+            "-lxpgshim".into(),
+
+            // LLVM will insert calls to the stack protector functions
+            // "__stack_chk_fail" and "__stack_chk_guard" into code in native
+            // object files.  Some platforms include these symbols directly in
+            // libc, but at least historically these have been provided in
+            // libssp.so on illumos and Solaris systems.
+            "-lssp".into(),
+
+            // Disable .SUNW_ldynsym sections on Solaris,
+            // as llvm cannot read the linked SHT_SYMTAB_SHNDX
+            "-Wl,-znoldynsym".into(),
+        ],
+    );
+
+    let mut late_link_args_dynamic = LinkArgs::new();
+    late_link_args_dynamic.insert(
+        LinkerFlavor::Unix(Cc::Yes),
+        vec![
+            // For shared libraries, we force resolution so the resulting
+            // .so receives a DT_NEEDED entry for libxpgshim.
+            "-Wl,-u,__sysconf_xpg7".into(),
+            "-lxpgshim".into(),
+        ],
+    );
+
     TargetOptions {
         os: Os::Solaris,
         dynamic_linking: true,
@@ -10,6 +27,9 @@
         linker_flavor: LinkerFlavor::Unix(Cc::Yes),
         limit_rdylib_exports: false, // Linker doesn't support this
         eh_frame_header: false,
+        frame_pointer: FramePointer::Always,
+        late_link_args,
+        late_link_args_dynamic,
 
         ..Default::default()
     }
--- rustc-1.91.0-src/src/bootstrap/src/core/builder/cargo.rs.~1~	2025-10-31 19:09:10.903162327 +0000
+++ rustc-1.91.0-src/src/bootstrap/src/core/builder/cargo.rs	2025-10-31 19:11:10.099136509 +0000
@@ -279,16 +279,20 @@
                 && !target.contains("aix")
                 && !target.contains("xous")
             {
-                self.rustflags.arg("-Clink-args=-Wl,-z,origin");
+                self.rustflags.arg("-Clink-arg=-Wl,-z,origin");
                 Some(format!("-Wl,-rpath,$ORIGIN/../{libdir}"))
             } else {
                 None
             };
             if let Some(rpath) = rpath {
-                self.rustflags.arg(&format!("-Clink-args={rpath}"));
+                self.rustflags.arg(&format!("-Clink-arg={rpath}"));
             }
         }
 
+        // Disable .SUNW_ldynsym sections on Solaris,
+        // as llvm cannot read the linked SHT_SYMTAB_SHNDX
+        self.rustflags.arg("-Clink-arg=-Wl,-znoldynsym");
+
         for arg in linker_args(builder, compiler.host, LldThreads::Yes) {
             self.hostflags.arg(&arg);
         }
